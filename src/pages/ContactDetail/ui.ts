import { styled, Box } from '@mui/material';

export const Container = styled(Box)(() => ({
  flex: 1,
  display: 'flex',
  backgroundColor: 'white',
}));

export const RightContainer = styled(Box)(() => ({ padding: 24, width: 400 }));
