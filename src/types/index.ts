export enum ACTIVITY_TYPE_ID {
  EMAIL = 1,
}

export enum ACTIVITY_STATUS {
  SENT = 'Sent',
  REPLIED = 'Replied',
  OPENED = 'Opend',
  SPAM = 'In Spam',
}

export enum SALE_PHASE_ID {
  PRESALES = 1,
  DEAL = 2,
  POSTSALES = 3,
}

export enum CONTACT_STAGE_ID {
  COLD = 1,
  WARM = 2,
  HOT = 3,
  CUSTOMER = 4,
}

export enum EMAIL_TYPE_ID {
  INCOMING = 1,
  OUTGOING = 2,
}
